spring:
  application:
    name: moida-backend
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 100
    open-in-view: false

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 50MB

  jackson:
    serialization:
      write-dates-as-timestamps: false
    time-zone: Asia/Seoul

server:
  port: 9000
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

jwt:
  secret: ${JWT_SECRET:moida-local-secret-key-must-be-at-least-32-bytes-long}
  access-token-validity: 3600000
  refresh-token-validity: 1209600000

logging:
  level:
    org.hibernate.SQL: info
    org.hibernate.orm.jdbc.bind: info
    com.moida: debug
social:
  kakao:
    client-id: ${KAKAO_CLIENT_ID:mock-kakao-client-id}
    redirect-uri: ${KAKAO_REDIRECT_URI:http://localhost:3000/login/oauth2/code/kakao}
  naver:
    client-id: ${NAVER_CLIENT_ID:mock-naver-client-id}
    client-secret: ${NAVER_CLIENT_SECRET:mock-naver-client-secret}
    redirect-uri: ${NAVER_REDIRECT_URI:http://localhost:3000/login/oauth2/code/naver}
  google:
    client-id: ${GOOGLE_CLIENT_ID:mock-google-client-id}
    client-secret: ${GOOGLE_CLIENT_SECRET:mock-google-client-secret}
    redirect-uri: ${GOOGLE_REDIRECT_URI:http://localhost:3000/login/oauth2/code/google}